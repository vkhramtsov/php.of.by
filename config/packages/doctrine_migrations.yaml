doctrine_migrations:
    migrations_paths:
        Application\Migrations: '%kernel.project_dir%/db/DoctrineMigrations'
    storage:
        table_storage:
            table_name: 'migration_versions'
            version_column_length: 255
